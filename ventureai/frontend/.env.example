# Base URL for the VentureAI FastAPI backend.
# Leave blank in dev — vite.config.ts proxies /evaluate, /status, /memo to :8000.
# Set this in production, e.g. https://api.ventureai.example.com
VITE_API_URL=
